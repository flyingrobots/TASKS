{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "waves.json",
  "type": "object",
  "required": ["meta", "waves"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["version", "planID", "artifactHash"],
      "additionalProperties": false,
      "properties": {
        "version": {"type": "string", "minLength": 1},
        "planID": {"type": "string", "minLength": 1},
        "artifactHash": {
          "type": "string",
          "minLength": 64,
          "maxLength": 64,
          "pattern": "^[A-Fa-f0-9]{64}$"
        }
      }
    },
    "waves": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "array",
        "minItems": 1,
        "items": {"type": "string"}
      }
    }
  },
  "additionalProperties": false
}
